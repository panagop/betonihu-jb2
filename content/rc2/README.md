# MyST Markdown export

## Structure

- `docs/`: all MyST Markdown files
- `images/`: extracted images from the original DOCX

Open `docs/index.md` as the entry point.

This export keeps one Markdown file per heading/subsection found in the original Word document.
